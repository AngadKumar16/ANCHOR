{
    "__comment": "Edit values below. Leave xcode_workspace or xcode_project blank as appropriate. Keep dry_run true until you review generated patches. If you use CocoaPods set xcode_workspace to the .xcworkspace created by `pod install`. Configuration for automatic code improvement and feature addition",
    "anchor_dir": "ANCHOR",
    "small_file_line_threshold": 200,
    "iterations_before_run": 5,
    "max_iterations": 50,
    "backup_dir": ".improver_backups",
    "dry_run": false,
    "auto_apply": true,
    "auto_commit": true,
    "auto_push": true,
    "push_branch": "main",
    "create_pr": false,
    "confidence_threshold": 0.7,
    "feature_branch_prefix": "improver",
    "xcode_workspace": "./ANCHOR.xcodeproj/project.xcworkspace",
    "xcode_project": "",
    "xcode_scheme": "ANCHOR",
    "__note_find_scheme": "Run the xcodebuild -list command (see chat) and paste the scheme name here or into the repo config before running the improver.",
    "xcode_configuration": "Debug",
    "simulator_name": "iPhone 16",
    "simulator_os": "18.4",
    "gemini_cli": "gemini",
    "gh_cli": "gh",
    "cocoapods": true,
    "max_retry_attempts": 3,
    "retry_delay_seconds": 10,
    "notes": "Auto-apply and auto-push enabled. Changes will be committed and pushed to main branch. Replace MyApp.xcworkspace and MyAppTests with your actual workspace/scheme. Set dry_run=false only after you inspect suggested patches."
}